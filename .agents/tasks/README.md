# Agent Task Queue

Use this folder for future task notes if work is split across agents.

Current next implementation tasks:

1. Add catalog validation script.
2. Make Jenkins read `services.yaml` for deployable modules.
3. Implement Docker Hub image build/push stages.
4. Build GitOps overlays with Docker Hub image overrides.
5. Implement parameterized Jenkins CD jobs.
6. Run real cluster setup and collect evidence.
